<UserControl x:Class="SmartShop.Components.ProductCard"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Border Background="White" CornerRadius="12" Padding="12" Width="320" Margin="0,4"
            BorderBrush="#E0E0E0" BorderThickness="1">
        <StackPanel>
            <Image x:Name="ProductImage" Height="150" Stretch="UniformToFill">
                <Image.Clip>
                    <RectangleGeometry Rect="0,0,296,150" RadiusX="8" RadiusY="8"/>
                </Image.Clip>
            </Image>
            <TextBlock x:Name="NameText" FontWeight="SemiBold" FontSize="15"
                       TextWrapping="Wrap" MaxHeight="40"
                       TextTrimming="CharacterEllipsis" Margin="0,8,0,0"/>
            <DockPanel Margin="0,6,0,0">
                <TextBlock x:Name="PriceText" FontSize="18" Foreground="#E64545"
                           FontWeight="Bold" DockPanel.Dock="Left"/>
                <TextBlock x:Name="RatingText" Foreground="#FF8C00" FontSize="13"
                           HorizontalAlignment="Right"/>
            </DockPanel>
        </StackPanel>
    </Border>
</UserControl>
