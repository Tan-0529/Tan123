# M2 LLM 对话 + SSE 流式 实现计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 在 M1 检索能力之上，构建「检索 → Prompt 组装 → LLM 流式生成 → SSE 推送」的端到端对话闭环，支持多轮上下文记忆。

**Architecture:** 新增 LLM 网关（抽象 + OpenAI 兼容流式 + Fake）、会话记忆（InMemory）、对话编排器（整合检索/记忆/LLM）、SSE 端点。均通过接口隔离，LLM 生产走 OpenAI 兼容 API，测试走 FakeLLM。

**Tech Stack:** 复用 M1 依赖；新增无（OpenAI SDK 已有）。

**Spec:** `C:\Users\lenovo\Documents\Agent项目\SmartShop-AI-实施方案.md`（§2.3 大模型集成、§2.4 流式交互）

## Global Constraints

- 后端代码位于 `backend/app`，测试 `backend/tests`
- LLM 走 OpenAI 兼容 API：`openai_base_url` + `openai_api_key` + `llm_model`（默认 `gpt-4o`）
- LLM 流式 API 统一用 `AsyncOpenAI().chat.completions.create(stream=True)`
- 会话记忆用 InMemory 实现（进程内 dict），接口预留 Redis 切换
- SSE 事件类型：`meta` / `delta` / `done` / `error`（card/intent 留待 M3/M4）
- 测试不依赖真实 LLM，用 `FakeLLM`
- 提交格式沿用 M1

---

### Task 1: 配置扩展（LLM 参数）

**Files:**
- Modify: `backend/app/config.py`
- Modify: `backend/tests/test_config.py`

**Interfaces:**
- Produces: `Settings.llm_model`（默认 `"gpt-4o"`）

- [ ] **Step 1: 写失败测试**

`backend/tests/test_config.py` 追加：
```python
def test_settings_llm_model_default():
    settings = Settings(_env_file=None)
    assert settings.llm_model == "gpt-4o"
```

- [ ] **Step 2: 跑测试确认失败**

Run: `backend/.venv/Scripts/python.exe -m pytest tests/test_config.py::test_settings_llm_model_default -v`

- [ ] **Step 3: 写实现**

`backend/app/config.py` 追加字段：
```python
    llm_model: str = "gpt-4o"
```

- [ ] **Step 4: 跑测试确认通过**

- [ ] **Step 5: 提交**

```bash
git add backend/app/config.py backend/tests/test_config.py
git commit -m "feat: add llm_model config"
```

---

### Task 2: LLM 网关（抽象 + OpenAI 流式 + Fake）

**Files:**
- Create: `backend/app/ai/llm.py`
- Modify: `backend/app/ai/__init__.py`
- Test: `backend/tests/test_llm.py`

**Interfaces:**
- Consumes: `app.config.Settings`
- Produces:
  - `LLM`（抽象，`async stream_chat(messages: list[dict]) -> AsyncIterator[str]`）
  - `OpenAILLM(Settings)`（真实流式）
  - `FakeLLM(response: str, chunk_size: int = 4)`（测试）

- [ ] **Step 1: 写失败测试**

`backend/tests/test_llm.py`:
```python
import pytest
from app.ai.llm import FakeLLM


@pytest.mark.asyncio
async def test_fake_llm_streams_full_response():
    llm = FakeLLM("你好，我是导购", chunk_size=2)
    chunks = [c async for c in llm.stream_chat([])]
    assert "".join(chunks) == "你好，我是导购"
    assert len(chunks) > 1
```

- [ ] **Step 2: 跑测试确认失败**

- [ ] **Step 3: 写实现**

`backend/app/ai/llm.py`:
```python
from abc import ABC, abstractmethod
from collections.abc import AsyncIterator

from openai import AsyncOpenAI

from app.config import Settings


class LLM(ABC):
    @abstractmethod
    def stream_chat(self, messages: list[dict]) -> AsyncIterator[str]: ...


class OpenAILLM(LLM):
    def __init__(self, settings: Settings):
        self._model = settings.llm_model
        self._base_url = settings.openai_base_url
        self._api_key = settings.openai_api_key
        self._client = None

    def _get_client(self) -> AsyncOpenAI:
        if self._client is None:
            self._client = AsyncOpenAI(base_url=self._base_url, api_key=self._api_key)
        return self._client

    async def stream_chat(self, messages: list[dict]) -> AsyncIterator[str]:
        stream = await self._get_client().chat.completions.create(
            model=self._model, messages=messages, stream=True, temperature=0.7,
        )
        async for chunk in stream:
            if chunk.choices and chunk.choices[0].delta.content:
                yield chunk.choices[0].delta.content


class FakeLLM(LLM):
    def __init__(self, response: str, chunk_size: int = 4):
        self._response = response
        self._chunk_size = chunk_size

    async def stream_chat(self, messages: list[dict]) -> AsyncIterator[str]:
        for i in range(0, len(self._response), self._chunk_size):
            yield self._response[i:i + self._chunk_size]
```

`backend/app/ai/__init__.py` 追加：
```python
from app.ai.llm import LLM, OpenAILLM, FakeLLM

__all__ = ["Embedder", "OpenAIEmbedder", "FakeEmbedder", "LLM", "OpenAILLM", "FakeLLM"]
```

- [ ] **Step 4: 跑测试确认通过**

- [ ] **Step 5: 提交**

```bash
git add backend/app/ai backend/tests/test_llm.py
git commit -m "feat: add LLM abstraction with OpenAI streaming and fake impls"
```

---

### Task 3: Prompt 管理（System Prompt + 消息组装）

**Files:**
- Create: `backend/app/ai/prompt.py`
- Test: `backend/tests/test_prompt.py`

**Interfaces:**
- Produces:
  - `SYSTEM_PROMPT: str`
  - `build_messages(history: list[dict], context: str, query: str) -> list[dict]`

- [ ] **Step 1: 写失败测试**

`backend/tests/test_prompt.py`:
```python
from app.ai.prompt import SYSTEM_PROMPT, build_messages


def test_build_messages_order():
    msgs = build_messages(
        history=[{"role": "user", "content": "你好"}, {"role": "assistant", "content": "你好"}],
        context="【检索上下文】\n商品A", query="有沙发吗",
    )
    assert msgs[0]["role"] == "system"
    assert msgs[-1]["role"] == "user"
    assert msgs[-1]["content"] == "有沙发吗"
    assert any("检索上下文" in m["content"] for m in msgs)


def test_system_prompt_mentions_guide():
    assert "导购" in SYSTEM_PROMPT
```

- [ ] **Step 2: 跑测试确认失败**

- [ ] **Step 3: 写实现**

`backend/app/ai/prompt.py`:
```python
SYSTEM_PROMPT = """你是「SmartShop AI」的资深导购顾问，拥有 10 年家居/消费零售经验。

【角色定位】你温暖、专业、有同理心，像一位懂行又耐心的资深导购。

【核心原则】
1. 基于事实：只依据 <检索上下文> 提供的商品信息回答，绝不编造价格、参数、库存或链接。
   若上下文中没有足够信息，坦诚告知并给出进一步澄清的问题。
2. 决策辅助：不满足于罗列商品，要帮用户做"对比→权衡→推荐"，明确说明推荐理由与适用场景。
3. 引导澄清：当用户需求模糊（预算、尺寸、风格、场景）时，用不超过 2 个关键问题主动澄清。
4. 同理心：理解用户的预算焦虑与决策纠结，语气不生硬，不推销。

【输出风格】中文，简洁分段，先给结论再展开。金额统一使用「¥」符号，价格保留两位小数。
"""


def build_messages(history: list[dict], context: str, query: str) -> list[dict]:
    msgs = [{"role": "system", "content": SYSTEM_PROMPT}]
    msgs.extend(history)
    if context:
        msgs.append({"role": "system", "content": f"<检索上下文>\n{context}\n</检索上下文>"})
    msgs.append({"role": "user", "content": query})
    return msgs
```

- [ ] **Step 4: 跑测试确认通过**

- [ ] **Step 5: 提交**

```bash
git add backend/app/ai/prompt.py backend/tests/test_prompt.py
git commit -m "feat: add system prompt and message builder"
```

---

### Task 4: 会话记忆（InMemory，多轮）

**Files:**
- Create: `backend/app/core/__init__.py`
- Create: `backend/app/core/memory.py`
- Test: `backend/tests/test_memory.py`

**Interfaces:**
- Produces:
  - `MemoryStore`（抽象：`get_history(cid) -> list[dict]`, `add_turn(cid, role, content) -> None`）
  - `InMemoryMemory(max_turns=20)`

- [ ] **Step 1: 写失败测试**

`backend/tests/test_memory.py`:
```python
from app.core.memory import InMemoryMemory


def test_memory_add_and_get():
    m = InMemoryMemory()
    m.add_turn("c1", "user", "你好")
    m.add_turn("c1", "assistant", "你好，想买什么？")
    h = m.get_history("c1")
    assert len(h) == 2
    assert h[0]["role"] == "user"


def test_memory_isolated_by_conversation():
    m = InMemoryMemory()
    m.add_turn("c1", "user", "a")
    m.add_turn("c2", "user", "b")
    assert len(m.get_history("c1")) == 1
    assert len(m.get_history("c2")) == 1
```

- [ ] **Step 2: 跑测试确认失败**

- [ ] **Step 3: 写实现**

`backend/app/core/memory.py`:
```python
from abc import ABC, abstractmethod
from collections import defaultdict, deque


class MemoryStore(ABC):
    @abstractmethod
    def get_history(self, conversation_id: str) -> list[dict]: ...

    @abstractmethod
    def add_turn(self, conversation_id: str, role: str, content: str) -> None: ...


class InMemoryMemory(MemoryStore):
    def __init__(self, max_turns: int = 20):
        self._max_turns = max_turns
        self._data: dict[str, deque] = defaultdict(
            lambda: deque(maxlen=max_turns)
        )

    def get_history(self, conversation_id: str) -> list[dict]:
        return list(self._data[conversation_id])

    def add_turn(self, conversation_id: str, role: str, content: str) -> None:
        self._data[conversation_id].append({"role": role, "content": content})
```

`backend/app/core/__init__.py`:
```python
from app.core.memory import InMemoryMemory, MemoryStore

__all__ = ["InMemoryMemory", "MemoryStore"]
```

- [ ] **Step 4: 跑测试确认通过**

- [ ] **Step 5: 提交**

```bash
git add backend/app/core backend/tests/test_memory.py
git commit -m "feat: add in-memory conversation memory store"
```

---

### Task 5: 对话编排器（整合检索 + 记忆 + LLM）

**Files:**
- Create: `backend/app/core/orchestrator.py`
- Test: `backend/tests/test_orchestrator.py`

**Interfaces:**
- Consumes: `Retriever`, `LLM`, `MemoryStore`
- Produces:
  - `Orchestrator(retriever, llm, memory).stream_chat(conversation_id, message) -> AsyncIterator[dict]`
    （产出事件 dict：`{"event": "meta"|"delta"|"done"|"error", ...}`）

- [ ] **Step 1: 写失败测试**

`backend/tests/test_orchestrator.py`:
```python
import pytest
from app.ai.embedding import FakeEmbedder
from app.ai.llm import FakeLLM
from app.core.memory import InMemoryMemory
from app.core.orchestrator import Orchestrator
from app.db.vector_store import InMemoryVectorStore
from app.rag.retriever import Retriever


@pytest.mark.asyncio
async def test_stream_chat_emits_delta_and_done():
    store = InMemoryVectorStore()
    retriever = Retriever(store, FakeEmbedder(dim=8))
    orch = Orchestrator(retriever, FakeLLM("这是推荐"), InMemoryMemory())
    events = [e async for e in orch.stream_chat("c1", "有沙发吗")]
    kinds = [e["event"] for e in events]
    assert "delta" in kinds
    assert kinds[-1] == "done"


@pytest.mark.asyncio
async def test_stream_chat_saves_history():
    mem = InMemoryMemory()
    orch = Orchestrator(Retriever(InMemoryVectorStore(), FakeEmbedder(dim=8)),
                        FakeLLM("好"), mem)
    async for _ in orch.stream_chat("c1", "你好"):
        pass
    assert len(mem.get_history("c1")) == 2
```

- [ ] **Step 2: 跑测试确认失败**

- [ ] **Step 3: 写实现**

`backend/app/core/orchestrator.py`:
```python
import uuid
from collections.abc import AsyncIterator

from app.ai.llm import LLM
from app.ai.prompt import build_messages
from app.core.memory import MemoryStore
from app.rag.retriever import Retriever


def _format_context(results: list[dict]) -> str:
    lines = []
    for i, r in enumerate(results):
        lines.append(f"[{i + 1}] {r['text']}")
    return "\n".join(lines)


class Orchestrator:
    def __init__(self, retriever: Retriever, llm: LLM, memory: MemoryStore):
        self._retriever = retriever
        self._llm = llm
        self._memory = memory

    async def stream_chat(self, conversation_id: str, message: str) -> AsyncIterator[dict]:
        message_id = uuid.uuid4().hex
        yield {"event": "meta", "conversation_id": conversation_id, "message_id": message_id}

        history = self._memory.get_history(conversation_id)
        results = self._retriever.retrieve(message, top_k=5)
        context = _format_context(results)
        messages = build_messages(history, context, message)

        self._memory.add_turn(conversation_id, "user", message)

        full = ""
        try:
            async for token in self._llm.stream_chat(messages):
                full += token
                yield {"event": "delta", "content": token}
        except Exception as exc:
            yield {"event": "error", "message": str(exc)}
            return

        self._memory.add_turn(conversation_id, "assistant", full)
        yield {"event": "done"}
```

- [ ] **Step 4: 跑测试确认通过**

- [ ] **Step 5: 提交**

```bash
git add backend/app/core/orchestrator.py backend/tests/test_orchestrator.py
git commit -m "feat: add chat orchestrator wiring retrieval, memory, llm"
```

---

### Task 6: SSE 流式端点（POST /chat）

**Files:**
- Create: `backend/app/api/routes/chat.py`
- Modify: `backend/app/main.py`
- Test: `backend/tests/test_chat_api.py`

**Interfaces:**
- Consumes: `Orchestrator`
- Produces: `POST /chat` → `text/event-stream`（meta/delta/done）

- [ ] **Step 1: 写失败测试**

`backend/tests/test_chat_api.py`:
```python
from fastapi.testclient import TestClient

from app.ai.embedding import FakeEmbedder
from app.ai.llm import FakeLLM
from app.core.memory import InMemoryMemory
from app.db.vector_store import InMemoryVectorStore
from app.main import create_app
from app.rag.retriever import Retriever


def _client(monkeypatch):
    def _build_orchestrator():
        return None  # 占位，实际由 main 组装

    monkeypatch.setattr("app.main.build_embedder", lambda s: FakeEmbedder(dim=8))
    monkeypatch.setattr("app.main.build_store", lambda s: InMemoryVectorStore())
    monkeypatch.setattr("app.main.build_llm", lambda s: FakeLLM("推荐沙发给你"))
    return TestClient(create_app())


def test_chat_endpoint_streams(monkeypatch):
    client = _client(monkeypatch)
    with client.stream("POST", "/chat", json={"conversation_id": "c1", "message": "有沙发吗"}) as resp:
        assert resp.status_code == 200
        assert "text/event-stream" in resp.headers["content-type"]
        body = "".join(resp.iter_text())
        assert "data:" in body
```

- [ ] **Step 2: 跑测试确认失败**

- [ ] **Step 3: 写实现**

`backend/app/api/routes/chat.py`:
```python
import json

from fastapi import APIRouter, Request
from fastapi.responses import StreamingResponse
from pydantic import BaseModel

router = APIRouter()


class ChatRequest(BaseModel):
    conversation_id: str
    message: str


def _sse(event: str, data: dict) -> str:
    return f"event: {event}\ndata: {json.dumps(data, ensure_ascii=False)}\n\n"


@router.post("/chat")
async def chat(req: ChatRequest, request: Request):
    orchestrator = request.app.state.orchestrator

    async def event_stream():
        async for ev in orchestrator.stream_chat(req.conversation_id, req.message):
            yield _sse(ev["event"], {k: v for k, v in ev.items() if k != "event"})

    return StreamingResponse(
        event_stream(),
        media_type="text/event-stream",
        headers={"Cache-Control": "no-cache", "X-Accel-Buffering": "no"},
    )
```

`backend/app/main.py` 修改（新增 build_llm 与 orchestrator 组装）：
```python
from app.ai.llm import LLM, OpenAILLM
from app.core.memory import InMemoryMemory
from app.core.orchestrator import Orchestrator


def build_llm(settings: Settings) -> LLM:
    return OpenAILLM(settings)


def create_app() -> FastAPI:
    settings = Settings()
    store = build_store(settings)
    embedder = build_embedder(settings)
    llm = build_llm(settings)
    pipeline = IngestionPipeline(store, embedder)
    retriever = Retriever(store, embedder)
    memory = InMemoryMemory()
    orchestrator = Orchestrator(retriever, llm, memory)

    app = FastAPI(title="SmartShop AI")
    app.state.pipeline = pipeline
    app.state.retriever = retriever
    app.state.orchestrator = orchestrator

    from app.api.routes.ingest import router as ingest_router
    from app.api.routes.search import router as search_router
    from app.api.routes.chat import router as chat_router
    app.include_router(ingest_router)
    app.include_router(search_router)
    app.include_router(chat_router)
    return app
```

- [ ] **Step 4: 跑测试确认通过**

- [ ] **Step 5: 提交**

```bash
git add backend/app/api/routes/chat.py backend/app/main.py backend/tests/test_chat_api.py
git commit -m "feat: add SSE chat endpoint"
```

---

## Self-Review 记录

- **Spec 覆盖**：M2 范围（LLM 集成 + Prompt + 多轮记忆 + 流式对话）已覆盖；多模态/商品卡片/意图识别属 M3-M4。
- **类型一致性**：`LLM.stream_chat`、`MemoryStore`、`Orchestrator.stream_chat`、`build_messages` 签名一致。
- **已知简化**：`OpenAILLM` 暂不处理 `function calling`（M4 商品卡片时加）；SSE 事件不含 `card`/`intent`。
- **注意**：`build_llm` 用 `OpenAILLM`，与 M1 的 `OpenAIEmbedder` 一样采用延迟初始化（避免空 key 时 import 崩溃）。
